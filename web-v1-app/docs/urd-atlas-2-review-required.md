# Review required

This branch requires local validation before merge.

Recommended command sequence:

```bash
cd web-v1-app
npm run lint
npm run typecheck
npm run build
```

Then manually inspect:

- `/`
- `/explorer`
- `/analyst-kit`
- `/workflows`
- `/validation`
