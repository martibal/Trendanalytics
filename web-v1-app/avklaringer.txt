hvordan kan vi påstå at en endring er støy eller reell trend endring? hva slags beregning ligger til grunn for å kunne mene noe om det?

få inn infobokser per json som viser nøkler med info til hver nøkkel



npm test -- --runInBand src/app/api/v1/status/route.test.ts


Landingsside fix;

- fjerne view live chains fra landing
- legge inn en mer synlig og tilgjengelig link for oversikt over json filene. gjør dem klikkbare/utvidbare så brukeren kan kollapse og utvide
feks meta og trykke på hver enkelt nøkkel og se beskrivelse. feks en popup infoboks med mer inngående forklaring på json filene. feks "what do
the json files contain" eller noe lignende, så en popup infoboks med litt mer info. også kanskje en infolink der med "click here for more
specific json data"
- unngå bruk av ordet live siden dataene alltid er en dag på etterskudd
- trenger ikke live right now boksen i headeren siden det uansett kommer frem på linjen under. den boksen kan erstattes av noe annet
- chains boksen til venstre i bunn må fjernes, det er død link
- "who buys this" høres nesten nedlatedende ut, bytte ut med noe som er litt mer salgspitch aktig feks "this is for you if" eller noe lignende
- i bunn av landingssiden er det "what you actually get", de boksene bør være klikkbare, det er lite egen info som kommer frem i de boksene.
det bør være mulig fra flere steder å få spesifikk info om jsoin filene siden de er såpass sentrale for hele produktet