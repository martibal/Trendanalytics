
# 🔧 MASTER PROMPT – “LANDING STYLE TRANSFORM”

Bruk denne sammen med *én konkret fil*.

---

You are modifying an existing Next.js (App Router) TypeScript page.

CRITICAL RULES (must not be violated):

* Do NOT remove functionality.
* Do NOT shorten the file.
* Do NOT restructure data fetching logic.
* Do NOT introduce new dependencies.
* Do NOT break TypeScript.
* Return ONE complete file only.
* No diff format. No explanations. Only final file.

---

## OBJECTIVE

Transform this page so it visually and structurally matches the Urd Atlas landing page design system.

This is NOT a redesign. It is a strict visual + UX normalization.

---

## GLOBAL DESIGN SYSTEM (MANDATORY)

1. COLOR SYSTEM

* Background: bg-[#edf6ff]
* Primary text: text-[#0a1d3a]
* Secondary text: text-[#37547b]
* Muted text: text-[#557099]
* Borders: border-[#c9d9ea]
* Soft panels: bg-[#eaf3fb]
* Highlight: cyan / emerald ONLY for meaning (not decoration)

NO random gradients, NO heavy glow, NO dark UI sections.

---

2. CARD SYSTEM (VERY IMPORTANT)

ALL UI blocks must follow this exact pattern:

rounded-2xl border border-[#c9d9ea] bg-white p-4

or for softer sections:

rounded-2xl border border-[#c9d9ea] bg-[#eaf3fb] p-4

REMOVE:

* heavy shadows
* glassmorphism
* neon effects
* large spacing gaps

Goal: HIGH DATA DENSITY

---

3. TYPOGRAPHY HIERARCHY

Section label:
text-xs uppercase tracking-[0.14em] text-[#557099]

Section title:
text-2xl or text-3xl font-semibold text-[#0a1d3a]

Body:
text-sm leading-7 text-[#37547b]

---

4. BUTTONS / LINKS

Primary:
rounded-full border border-[#c9d9ea] bg-[#eef6ff] px-3 py-1 text-xs text-[#0d2447]

Secondary:
text-[#557099] hover:text-[#37547b]

NO large CTA buttons inside data sections.

---

5. REMOVE “UI NOISE”

Eliminate:

* oversized paddings
* empty whitespace blocks
* duplicated headers
* decorative containers with no data

Everything must feel like:
→ compact
→ analytical
→ information-first

---

6. DATA FIRST LAYOUT

Each section must follow:

[small label]
[clear title]
[data immediately visible]
[optional explanation BELOW data]

NOT:
title → paragraph → explanation → data

---

7. MODALS / INFO

Replace ALL :target CSS modal logic with controlled state (client component if needed).

Rules:

* Click must open immediately (no refresh)
* No anchor-hash navigation
* No broken overlay behavior

---

8. CHART SECTIONS

Each chart block must be:

Header (metric name + units)
→ Chart
→ 2 small info blocks:

[How to read]
[Why this metric]

Both compact, not verbose.

---

9. SCORE / METRICS

Numbers must be visually dominant:
text-3xl or text-4xl font-semibold

Labels small above.

---

10. CONSISTENCY

Landing page style is:

* calm
* flat
* minimal
* dense
* professional (not flashy)

Match that EXACTLY.

---

## TASK

Apply all rules above to the provided file.

Do NOT invent new features.
Do NOT change logic.
Only adjust structure, layout, and styling.

---

## OUTPUT

Return the FULL updated file.
Nothing else.

---


Denne malen er designet for å unngå:

* at modellen blåser opp filstørrelsen ❌
* at den lager ny arkitektur ❌
* at den bryter hooks / async ❌
* at den “designer fritt” ❌

---


