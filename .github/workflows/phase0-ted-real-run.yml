name: Phase 0 TED Real Outcome Run

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Runner parked while frozen v7 artifact is staged."
