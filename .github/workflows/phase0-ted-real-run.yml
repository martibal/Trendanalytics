name: Phase 0 TED Real Outcome Run v8

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  phase0:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Runner intentionally parked while v8 privacy-safe transport is frozen."
