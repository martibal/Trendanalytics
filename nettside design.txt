OVERORDNET DESIGNFILOSOFI
Mål: Premium, rolig, sofistikert dashboard som kommuniserer "profesjonell innsikt uten stress"
Referanser å studere:

Stripe Dashboard (cleanness + spacing)
Linear app (typography + hierarchy)
Vercel Dashboard (subtle colors + depth)
Apple.com (whitespace + elegance)


FARGE SYSTEM (DETALJERT)
Primær palett (beholdes og utvides)
css/* Dark background system */
--bg-primary: #0A0E1A;        /* Main background */
--bg-secondary: #141825;       /* Cards/elevated elements */
--bg-tertiary: #1D2235;        /* Hover states */
--bg-quaternary: #262D45;      /* Active/selected */

/* Accent colors (nøytrale, ikke anxiety-inducing) */
--accent-primary: #3B82F6;     /* Blue - primary actions */
--accent-secondary: #6366F1;   /* Indigo - secondary elements */
--accent-subtle: #1E3A8A;      /* Dark blue - subtle highlights */

/* Text hierarchy */
--text-primary: #F9FAFB;       /* Headings, important text */
--text-secondary: #D1D5DB;     /* Body text */
--text-tertiary: #9CA3AF;      /* Labels, metadata */
--text-muted: #6B7280;         /* Timestamps, least important */

/* Status colors (IKKE rød/gul for regime) */
--status-info: #3B82F6;        /* Normal/Stable/Info */
--status-success: #10B981;     /* High confidence only */
--status-warning: #F59E0B;     /* KUN for low confidence */
--status-neutral: #6B7280;     /* Default state */

/* Borders and dividers */
--border-subtle: rgba(255, 255, 255, 0.06);
--border-medium: rgba(255, 255, 255, 0.10);
--border-strong: rgba(255, 255, 255, 0.15);

/* Overlays and shadows */
--overlay-light: rgba(0, 0, 0, 0.3);
--shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.3);
--shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4);
--shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.5);
--shadow-glow: 0 0 20px rgba(59, 130, 246, 0.15);
Gradient system (for depth og interesse)
css/* Subtle gradients for cards */
--gradient-card: linear-gradient(135deg, 
  rgba(59, 130, 246, 0.03) 0%, 
  rgba(99, 102, 241, 0.03) 100%);

/* Header gradient */
--gradient-header: linear-gradient(180deg,
  rgba(59, 130, 246, 0.08) 0%,
  rgba(10, 14, 26, 0) 100%);

/* Hover glow */
--gradient-glow: radial-gradient(circle at center,
  rgba(59, 130, 246, 0.1) 0%,
  transparent 70%);

TYPOGRAPHY SYSTEM (SVÆRT DETALJERT)
Font families
css/* Primary font - Moderne, lesbar */
--font-primary: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;

/* Monospace - for tall og kode */
--font-mono: 'JetBrains Mono', 'SF Mono', 'Monaco', 'Courier New', monospace;

/* Import from Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
Font sizes (scale basert på 16px base)
css--text-xs: 0.75rem;      /* 12px - timestamps, fine print */
--text-sm: 0.875rem;     /* 14px - labels, secondary info */
--text-base: 1rem;       /* 16px - body text */
--text-lg: 1.125rem;     /* 18px - emphasized text */
--text-xl: 1.25rem;      /* 20px - section headings */
--text-2xl: 1.5rem;      /* 24px - card titles */
--text-3xl: 1.875rem;    /* 30px - page title */
--text-4xl: 2.25rem;     /* 36px - hero text */
Font weights
css--font-light: 300;
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
Line heights
css--leading-tight: 1.2;     /* Headings */
--leading-snug: 1.4;      /* Subheadings */
--leading-normal: 1.6;    /* Body text */
--leading-relaxed: 1.8;   /* Long-form content */
Letter spacing
css--tracking-tight: -0.025em;   /* Large headings */
--tracking-normal: 0;         /* Body text */
--tracking-wide: 0.025em;     /* Small caps, labels */
--tracking-wider: 0.05em;     /* Button text */

SPACING SYSTEM (8px base grid)
css--space-1: 0.25rem;   /* 4px */
--space-2: 0.5rem;    /* 8px */
--space-3: 0.75rem;   /* 12px */
--space-4: 1rem;      /* 16px */
--space-5: 1.25rem;   /* 20px */
--space-6: 1.5rem;    /* 24px */
--space-8: 2rem;      /* 32px */
--space-10: 2.5rem;   /* 40px */
--space-12: 3rem;     /* 48px */
--space-16: 4rem;     /* 64px */
--space-20: 5rem;     /* 80px */
--space-24: 6rem;     /* 96px */

KOMPONENT-SPESIFIKK STYLING
1. HEADER / NAVIGATION BAR
css.header {
  background: var(--bg-secondary);
  border-bottom: 1px solid var(--border-subtle);
  backdrop-filter: blur(20px);
  position: sticky;
  top: 0;
  z-index: 100;
  padding: var(--space-4) var(--space-6);
  
  /* Subtle shadow */
  box-shadow: 0 1px 0 rgba(255, 255, 255, 0.03);
}

.header-inner {
  max-width: 1400px;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.logo {
  font-size: var(--text-xl);
  font-weight: var(--font-bold);
  color: var(--text-primary);
  letter-spacing: var(--tracking-tight);
  
  /* Subtle glow effect */
  text-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
}

.nav-links {
  display: flex;
  gap: var(--space-6);
}

.nav-link {
  font-size: var(--text-sm);
  font-weight: var(--font-medium);
  color: var(--text-secondary);
  text-decoration: none;
  padding: var(--space-2) var(--space-3);
  border-radius: 6px;
  transition: all 0.2s ease;
  letter-spacing: var(--tracking-wide);
}

.nav-link:hover {
  color: var(--text-primary);
  background: var(--bg-tertiary);
}

.nav-link.active {
  color: var(--accent-primary);
  background: rgba(59, 130, 246, 0.1);
}
2. HERO SECTION (Market Overview)
css.hero-section {
  background: var(--gradient-header);
  padding: var(--space-16) var(--space-6) var(--space-12);
  position: relative;
  overflow: hidden;
}

/* Subtle animated background effect */
.hero-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 100%;
  background: radial-gradient(circle at 20% 50%, 
    rgba(59, 130, 246, 0.05) 0%, 
    transparent 50%);
  animation: pulse 8s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 0.5; }
  50% { opacity: 1; }
}

.hero-inner {
  max-width: 1400px;
  margin: 0 auto;
  position: relative;
  z-index: 1;
}

.hero-title {
  font-size: var(--text-4xl);
  font-weight: var(--font-bold);
  color: var(--text-primary);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
  margin-bottom: var(--space-3);
}

.hero-subtitle {
  font-size: var(--text-lg);
  font-weight: var(--font-normal);
  color: var(--text-secondary);
  line-height: var(--leading-normal);
  max-width: 600px;
  margin-bottom: var(--space-8);
}

.hero-meta {
  display: flex;
  gap: var(--space-6);
  font-size: var(--text-sm);
  color: var(--text-tertiary);
}

.hero-meta-item {
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.hero-meta-label {
  font-weight: var(--font-medium);
  text-transform: uppercase;
  letter-spacing: var(--tracking-wider);
  font-size: var(--text-xs);
}

.hero-meta-value {
  color: var(--text-primary);
  font-weight: var(--font-semibold);
}
3. "AT A GLANCE" SUMMARY CARD (CRITICAL)
css.summary-card {
  background: var(--bg-secondary);
  border: 1px solid var(--border-medium);
  border-radius: 16px;
  padding: var(--space-8);
  margin: calc(var(--space-12) * -1) auto var(--space-12);
  max-width: 900px;
  position: relative;
  z-index: 10;
  
  /* Elevation */
  box-shadow: var(--shadow-lg), var(--shadow-glow);
  
  /* Subtle gradient overlay */
  background-image: var(--gradient-card);
}

.summary-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-6);
  padding-bottom: var(--space-4);
  border-bottom: 1px solid var(--border-subtle);
}

.summary-title {
  font-size: var(--text-2xl);
  font-weight: var(--font-bold);
  color: var(--text-primary);
  letter-spacing: var(--tracking-tight);
}

.summary-week {
  font-size: var(--text-sm);
  color: var(--text-tertiary);
  font-weight: var(--font-medium);
  font-variant-numeric: tabular-nums;
}

.summary-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: var(--space-6);
  margin-bottom: var(--space-6);
}

.summary-chain {
  padding: var(--space-4);
  background: rgba(255, 255, 255, 0.02);
  border-radius: 10px;
  border: 1px solid var(--border-subtle);
  transition: all 0.2s ease;
}

.summary-chain:hover {
  background: rgba(255, 255, 255, 0.04);
  border-color: var(--border-medium);
  transform: translateY(-2px);
}

.summary-chain-name {
  font-size: var(--text-sm);
  font-weight: var(--font-semibold);
  color: var(--text-tertiary);
  text-transform: uppercase;
  letter-spacing: var(--tracking-wider);
  margin-bottom: var(--space-2);
}

.summary-chain-status {
  font-size: var(--text-base);
  color: var(--text-primary);
  font-weight: var(--font-medium);
  margin-bottom: var(--space-1);
}

.summary-chain-detail {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  line-height: var(--leading-snug);
}

.summary-conclusion {
  padding: var(--space-5);
  background: rgba(59, 130, 246, 0.05);
  border: 1px solid rgba(59, 130, 246, 0.2);
  border-radius: 10px;
  margin-bottom: var(--space-6);
}

.summary-conclusion-text {
  font-size: var(--text-base);
  color: var(--text-primary);
  line-height: var(--leading-normal);
  margin: 0;
}

.summary-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: var(--space-4);
  border-top: 1px solid var(--border-subtle);
}

.confidence-badge {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-2) var(--space-3);
  background: rgba(16, 185, 129, 0.1);
  border: 1px solid rgba(16, 185, 129, 0.2);
  border-radius: 6px;
  font-size: var(--text-sm);
  font-weight: var(--font-medium);
  color: #10B981;
}

.next-update {
  font-size: var(--text-sm);
  color: var(--text-tertiary);
}
4. CHAIN DETAIL CARDS
css.chain-cards-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: var(--space-6);
  display: flex;
  flex-direction: column;
  gap: var(--space-12);
}

.chain-card {
  background: var(--bg-secondary);
  border: 1px solid var(--border-medium);
  border-radius: 12px;
  padding: var(--space-8);
  transition: all 0.3s ease;
}

.chain-card:hover {
  border-color: var(--border-strong);
  box-shadow: var(--shadow-md);
}

.chain-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: var(--space-6);
}

.chain-title-group {
  display: flex;
  align-items: center;
  gap: var(--space-3);
}

.chain-icon {
  width: 40px;
  height: 40px;
  border-radius: 10px;
  background: var(--bg-tertiary);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--text-xl);
}

.chain-name {
  font-size: var(--text-2xl);
  font-weight: var(--font-bold);
  color: var(--text-primary);
  letter-spacing: var(--tracking-tight);
}

.chain-status-pill {
  padding: var(--space-2) var(--space-4);
  background: rgba(59, 130, 246, 0.1);
  border: 1px solid rgba(59, 130, 246, 0.2);
  border-radius: 20px;
  font-size: var(--text-sm);
  font-weight: var(--font-medium);
  color: var(--accent-primary);
  letter-spacing: var(--tracking-wide);
}

/* KRITISK: Fjern røde/gule farger fra status */
.chain-status-pill[data-status="stable"],
.chain-status-pill[data-status="normal"],
.chain-status-pill[data-status="heating"],
.chain-status-pill[data-status="congested"] {
  background: rgba(59, 130, 246, 0.1);
  border-color: rgba(59, 130, 246, 0.2);
  color: var(--accent-primary);
}

/* KUN gul for low confidence */
.chain-status-pill[data-status="low-confidence"] {
  background: rgba(245, 158, 11, 0.1);
  border-color: rgba(245, 158, 11, 0.2);
  color: #F59E0B;
}
5. METRICS GRID
css.metrics-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: var(--space-4);
  margin-bottom: var(--space-8);
}

.metric-card {
  background: rgba(255, 255, 255, 0.02);
  border: 1px solid var(--border-subtle);
  border-radius: 10px;
  padding: var(--space-5);
  transition: all 0.2s ease;
}

.metric-card:hover {
  background: rgba(255, 255, 255, 0.04);
  border-color: var(--border-medium);
}

.metric-label {
  font-size: var(--text-sm);
  color: var(--text-tertiary);
  font-weight: var(--font-medium);
  margin-bottom: var(--space-2);
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.metric-value {
  font-size: var(--text-2xl);
  font-weight: var(--font-bold);
  color: var(--text-primary);
  font-variant-numeric: tabular-nums;
  font-family: var(--font-mono);
  margin-bottom: var(--space-1);
}

.metric-change {
  font-size: var(--text-sm);
  font-weight: var(--font-medium);
  color: var(--text-secondary);
  display: flex;
  align-items: center;
  gap: var(--space-1);
}

.metric-change-positive {
  color: var(--status-info); /* Blå, ikke grønn */
}

.metric-change-negative {
  color: var(--text-tertiary); /* Grå, ikke rød */
}

.metric-subtext {
  font-size: var(--text-xs);
  color: var(--text-muted);
  margin-top: var(--space-2);
}
6. CHART CONTAINERS
css.chart-section {
  margin-top: var(--space-8);
  padding: var(--space-6);
  background: rgba(255, 255, 255, 0.01);
  border-radius: 12px;
  border: 1px solid var(--border-subtle);
}

.chart-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-6);
}

.chart-title {
  font-size: var(--text-lg);
  font-weight: var(--font-semibold);
  color: var(--text-primary);
}

.chart-description {
  font-size: var(--text-sm);
  color: var(--text-tertiary);
  margin-top: var(--space-1);
  max-width: 600px;
}

.chart-container {
  position: relative;
  height: 300px;
  margin-bottom: var(--space-4);
}

/* Chart styling (assumes you're using a library like Chart.js) */
canvas {
  border-radius: 8px;
}

.chart-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: var(--space-4);
  border-top: 1px solid var(--border-subtle);
  font-size: var(--text-xs);
  color: var(--text-muted);
}
7. HISTORICAL ANALOG SECTION
css.historical-analog {
  background: rgba(99, 102, 241, 0.05);
  border: 1px solid rgba(99, 102, 241, 0.15);
  border-radius: 12px;
  padding: var(--space-6);
  margin-top: var(--space-8);
}

.analog-header {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  margin-bottom: var(--space-4);
}

.analog-icon {
  width: 32px;
  height: 32px;
  background: rgba(99, 102, 241, 0.1);
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #6366F1;
  font-size: var(--text-lg);
}

.analog-title {
  font-size: var(--text-lg);
  font-weight: var(--font-semibold);
  color: var(--text-primary);
}

.analog-match {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  margin-bottom: var(--space-4);
  padding: var(--space-3);
  background: rgba(0, 0, 0, 0.2);
  border-radius: 8px;
}

.analog-outcomes {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
  margin-bottom: var(--space-4);
}

.analog-outcome-item {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-size: var(--text-sm);
  color: var(--text-secondary);
}

.analog-outcome-icon {
  color: var(--accent-secondary);
}

.analog-disclaimer {
  padding: var(--space-3);
  background: rgba(0, 0, 0, 0.3);
  border-left: 3px solid rgba(99, 102, 241, 0.5);
  border-radius: 4px;
  font-size: var(--text-xs);
  color: var(--text-muted);
  font-style: italic;
}
8. CONFIDENCE INDICATOR
css.confidence-indicator {
  display: inline-flex;
  align-items: center;
  gap: var(--space-3);
  padding: var(--space-3) var(--space-4);
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid var(--border-subtle);
  border-radius: 8px;
}

.confidence-bar {
  width: 80px;
  height: 6px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 3px;
  overflow: hidden;
  position: relative;
}

.confidence-fill {
  height: 100%;
  background: linear-gradient(90deg, 
    var(--status-warning) 0%, 
    var(--status-info) 50%, 
    var(--status-success) 100%);
  transition: width 0.3s ease;
  border-radius: 3px;
}

.confidence-label {
  font-size: var(--text-sm);
  font-weight: var(--font-medium);
  color: var(--text-secondary);
}

.confidence-value {
  font-family: var(--font-mono);
  font-size: var(--text-sm);
  font-weight: var(--font-semibold);
  color: var(--text-primary);
}

/* Expandable confidence details */
.confidence-details {
  margin-top: var(--space-4);
  padding: var(--space-4);
  background: rgba(0, 0, 0, 0.2);
  border-radius: 8px;
  font-size: var(--text-sm);
  color: var(--text-secondary);
}

.confidence-details-header {
  font-weight: var(--font-semibold);
  color: var(--text-primary);
  margin-bottom: var(--space-2);
}

.confidence-details-list {
  list-style: none;
  padding: 0;
  margin: 0;
}

.confidence-details-item {
  padding: var(--space-2) 0;
  border-bottom: 1px solid var(--border-subtle);
}

.confidence-details-item:last-child {
  border-bottom: none;
}
9. BUTTONS AND INTERACTIVE ELEMENTS
css.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  padding: var(--space-3) var(--space-5);
  border-radius: 8px;
  font-size: var(--text-sm);
  font-weight: var(--font-medium);
  letter-spacing: var(--tracking-wide);
  transition: all 0.2s ease;
  cursor: pointer;
  border: none;
  text-decoration: none;
}

.btn-primary {
  background: var(--accent-primary);
  color: white;
  box-shadow: 0 0 0 0 rgba(59, 130, 246, 0);
}

.btn-primary:hover {
  background: #2563EB;
  box-shadow: 0 0 20px rgba(59, 130, 246, 0.4);
  transform: translateY(-1px);
}

.btn-secondary {
  background: rgba(255, 255, 255, 0.05);
  color: var(--text-primary);
  border: 1px solid var(--border-medium);
}

.btn-secondary:hover {
  background: rgba(255, 255, 255, 0.08);
  border-color: var(--border-strong);
}

.btn-ghost {
  background: transparent;
  color: var(--text-secondary);
}

.btn-ghost:hover {
  background: rgba(255, 255, 255, 0.05);
  color: var(--text-primary);
}

/* Toggle buttons for charts */
.toggle-group {
  display: inline-flex;
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid var(--border-subtle);
  border-radius: 8px;
  padding: var(--space-1);
}

.toggle-btn {
  padding: var(--space-2) var(--space-4);
  border-radius: 6px;
  font-size: var(--text-sm);
  font-weight: var(--font-medium);
  color: var(--text-secondary);
  background: transparent;
  border: none;
  cursor: pointer;
  transition: all 0.2s ease;
}

.toggle-btn.active {
  background: var(--accent-primary);
  color: white;
  box-shadow: var(--shadow-sm);
}

.toggle-btn:hover:not(.active) {
  color: var(--text-primary);
  background: rgba(255, 255, 255, 0.05);
}
10. COLLAPSIBLE SECTIONS
css.collapsible-section {
  border: 1px solid var(--border-subtle);
  border-radius: 10px;
  overflow: hidden;
  margin-bottom: var(--space-4);
  background: rgba(255, 255, 255, 0.01);
}

.collapsible-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--space-4) var(--space-5);
  cursor: pointer;
  transition: all 0.2s ease;
  user-select: none;
}

.collapsible-header:hover {
  background: rgba(255, 255, 255, 0.03);
}

.collapsible-title {
  font-size: var(--text-base);
  font-weight: var(--font-semibold);
  color: var(--text-primary);
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.collapsible-icon {
  transition: transform 0.3s ease;
  color: var(--text-tertiary);
}

.collapsible-section.expanded .collapsible-icon {
  transform: rotate(180deg);
}

.collapsible-content {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease, padding 0.3s ease;
}

.collapsible-section.expanded .collapsible-content {
  max-height: 5000px;
  padding: var(--space-5);
  border-top: 1px solid var(--border-subtle);
}

RESPONSIVE DESIGN (MOBILE FIRST)
Mobile (<768px)
css@media (max-width: 767px) {
  :root {
    /* Reduce spacing on mobile */
    --space-container: var(--space-4);
  }
  
  .hero-section {
    padding: var(--space-8) var(--space-4) var(--space-6);
  }
  
  .hero-title {
    font-size: var(--text-3xl);
  }
  
  .hero-subtitle {
    font-size: var(--text-base);
  }
  
  .summary-card {
    margin: calc(var(--space-8) * -1) var(--space-4) var(--space-8);
    padding: var(--space-5);
    border-radius: 12px;
  }
  
  .summary-grid {
    grid-template-columns: 1fr;
    gap: var(--space-4);
  }
  
  .chain-cards-container {
    padding: var(--space-4);
    gap: var(--space-8);
  }
  
  .chain-card {
    padding: var(--space-5);
  }
  
  .chain-name {
    font-size: var(--text-xl);
  }
  
  .metrics-grid {
    grid-template-columns: 1fr;
    gap: var(--space-3);
  }
  
  .chart-container {
    height: 250px;
  }
  
  /* Stack header elements vertically */
  .chain-header {
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-3);
  }
  
  /* Ensure touch targets are minimum 44px */
  .btn,
  .toggle-btn,
  .collapsible-header {
    min-height: 44px;
    min-width: 44px;
  }
  
  /* Increase base font size for readability */
  body {
    font-size: 16px; /* Never smaller */
  }
}
Tablet (768px - 1024px)
css@media (min-width: 768px) and (max-width: 1024px) {
  .summary-grid {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .metrics-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

ANIMATIONS AND TRANSITIONS
css/* Page load animation */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in-up {
  animation: fadeInUp 0.6s ease-out;
}

/* Stagger children animations */
.stagger-children > * {
  animation: fadeInUp 0.6s ease-out;
}

.stagger-children > *:nth-child(1) { animation-delay: 0.1s; }
.stagger-children > *:nth-child(2) { animation-delay: 0.2s; }
.stagger-children > *:nth-child(3) { animation-delay: 0.3s; }
.stagger-children > *:nth-child(4) { animation-delay: 0.4s; }

/* Skeleton loading states */
.skeleton {
  background: linear-gradient(
    90deg,
    rgba(255, 255, 255, 0.03) 25%,
    rgba(255, 255, 255, 0.06) 50%,
    rgba(255, 255, 255, 0.03) 75%
  );
  background-size: 200% 100%;
  animation: shimmer 2s infinite;
  border-radius: 6px;
}

@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}

/* Smooth scroll behavior */
html {
  scroll-behavior: smooth;
}

/* Hover elevation */
.elevate-on-hover {
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.elevate-on-hover:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}

ACCESSIBILITY
css/* Focus states */
*:focus-visible {
  outline: 2px solid var(--accent-primary);
  outline-offset: 2px;
  border-radius: 4px;
}

/* Skip to content link */
.skip-to-content {
  position: absolute;
  top: -100px;
  left: 0;
  background: var(--accent-primary);
  color: white;
  padding: var(--space-3) var(--space-5);
  text-decoration: none;
  z-index: 1000;
  border-radius: 0 0 8px 0;
}

.skip-to-content:focus {
  top: 0;
}

/* Screen reader only text */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}

/* Reduced motion */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}

SPECIFIC IMPROVEMENTS TO CURRENT DESIGN
Basert på bildene du sendte, her er konkrete endringer:
1. Market Overview Header (Bilde 1)
BEFORE (Current):

Tittel er litt liten
Metadata ikke tydelig nok separert

AFTER:
css.market-overview-header {
  padding: var(--space-20) var(--space-6) var(--space-12);
  background: var(--gradient-header);
}

.market-overview-title {
  font-size: 3rem; /* Større! */
  font-weight: 700;
  letter-spacing: -0.03em;
  margin-bottom: var(--space-4);
  background: linear-gradient(135deg, #F9FAFB 0%, #9CA3AF 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.market-overview-description {
  font-size: var(--text-lg);
  max-width: 700px;
  line-height: 1.7;
  color: var(--text-secondary);
}

.market-overview-status {
  display: inline-flex;
  align-items: center;
  gap: var(--space-3);
  padding: var(--space-3) var(--space-5);
  background: rgba(59, 130, 246, 0.1);
  border: 1px solid rgba(59, 130, 246, 0.3);
  border-radius: 30px;
  font-size: var(--text-lg);
  font-weight: 600;
  color: var(--accent-primary);
  margin-top: var(--space-6);
}
2. Ethereum Section (Bilde 2-3)
BEFORE:

Charts tar for mye plass
"How to read" teksten er ok, men kan være mer fremhevet

AFTER:
css/* Legg til collapse by default på charts */
.chain-charts {
  margin-top: var(--space-6);
}

.chart-group {
  border: 1px solid var(--border-subtle);
  border-radius: 12px;
  padding: var(--space-6);
  margin-bottom: var(--space-6);
  background: rgba(255, 255, 255, 0.01);
}

/* "How to read" fremheving */
.how-to-read-section {
  background: rgba(99, 102, 241, 0.06);
  border: 1px solid rgba(99, 102, 241, 0.2);
  border-left: 4px solid var(--accent-secondary);
  border-radius: 10px;
  padding: var(--space-5);
  margin-bottom: var(--space-8);
}

.how-to-read-title {
  font-size: var(--text-lg);
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: var(--space-3);
  display: flex;
  align-items: center;
  gap: var(--space-2);
}

.how-to-read-content {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  line-height: 1.7;
}

.how-to-read-content strong {
  color: var(--text-primary);
  font-weight: 600;
}
3. Charts (Bilde 3-4)
BEFORE:

Charts ok, men kan ha mer breathing room

AFTER:
css.chart-wrapper {
  padding: var(--space-8);
  background: rgba(0, 0, 0, 0.2);
  border-radius: 12px;
  margin: var(--space-6) 0;
}

.chart-canvas {
  margin: var(--space-4) 0;
}

/* Chart legend styling */
.chart-legend {
  display: flex;
  gap: var(--space-4);
  justify-content: center;
  padding-top: var(--space-4);
  border-top: 1px solid var(--border-subtle);
}

.chart-legend-item {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-size: var(--text-sm);
  color: var(--text-secondary);
}

.chart-legend-color {
  width: 12px;
  height: 12px;
  border-radius: 3px;
}
4. "What this means" boxes
css.interpretation-box {
  background: rgba(59, 130, 246, 0.04);
  border: 1px solid rgba(59, 130, 246, 0.15);
  border-radius: 10px;
  padding: var(--space-5);
  margin-top: var(--space-4);
}

.interpretation-title {
  font-size: var(--text-sm);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: var(--tracking-wider);
  color: var(--accent-primary);
  margin-bottom: var(--space-3);
}

.interpretation-content {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  line-height: 1.7;
}

.interpretation-list {
  list-style: none;
  padding: 0;
  margin: var(--space-3) 0 0 0;
}

.interpretation-list-item {
  padding-left: var(--space-4);
  position: relative;
  margin-bottom: var(--space-2);
}

.interpretation-list-item::before {
  content: '→';
  position: absolute;
  left: 0;
  color: var(--accent-primary);
  font-weight: 600;
}