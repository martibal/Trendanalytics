[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "urdatlas"
version = "0.1.0"
description = "Official lightweight Python client for the Urd Atlas HTTP/JSON API"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
